import {Application} from "../src/Application"

describe('Instaciate Class', () => {
    beforeEach(() => {
    });

    it('should  load movies', () => {
        // console.log((new Application).test());
      expect(5).toEqual(5);
    })
});